# Handle line endings automatically for files detected as text 
# and leave binary files untouched
* text=auto

# Force LF for shell scripts and maven wrapper to ensure they work in Docker/Linux
mvnw text eol=lf
*.sh text eol=lf
*.sql text eol=lf
*.xml text
*.java text
*.yml text
*.yaml text
*.properties text
