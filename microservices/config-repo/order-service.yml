server:
  port: 8085

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:sqlserver://sqlserver:1433;databaseName=csms_order;encrypt=false;trustServerCertificate=true
    username: sa
    password: Password123
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://eureka-server:8761/eureka/
